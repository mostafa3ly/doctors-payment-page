import React from 'react';
import classes from './Header.module.css';

const header = (props) => {
    return (
        <h1 className={classes.Header}>Subscription Payment</h1>
    );
}

export default header;